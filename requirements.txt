# MediChain Face Verification — Python Dependencies
# Python 3.11+
#
# !! DO NOT add torch or torchvision here !!
# They are installed via the CPU-only index in the Dockerfile:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.2.2+cpu
# Adding them here causes pip to pull the 2.4 GB CUDA build instead.

# ── Web framework ─────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Face detection + FaceNet ──────────────────────────────────────
# facenet-pytorch brings InceptionResnetV1 + MTCNN.
# It lists torch as a dependency but pip won't re-download it
# because torch+cpu is already satisfied from the Dockerfile step.
facenet-pytorch==2.5.3

# ── ML classifier ─────────────────────────────────────────────────
scikit-learn==1.4.2
numpy==1.26.4
scipy==1.13.0        # Required by scikit-learn SVM

# ── Image processing ──────────────────────────────────────────────
Pillow==10.3.0
# headless = no Qt/GTK GUI — smaller than opencv-python, same API
opencv-python-headless==4.9.0.80

# ── Firebase ──────────────────────────────────────────────────────
firebase-admin==6.5.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv==1.0.1
